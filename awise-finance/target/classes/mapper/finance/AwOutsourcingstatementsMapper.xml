<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awise.finance.mapper.AwOutsourcingstatementsMapper">
    
    <resultMap type="AwOutsourcingstatements" id="AwOutsourcingstatementsResult">
        <result property="outsourcingStatementsID"    column="outsourcingStatementsID"    />
        <result property="creator"    column="creator"    />
        <result property="reconciler"    column="reconciler"    />
        <result property="statementOfAccountDate"    column="statementOfAccountDate"    />
        <result property="notes"    column="notes"    />
        <result property="status"    column="status"    />
    </resultMap>

    <sql id="selectAwOutsourcingstatementsVo">
        select outsourcingStatementsID, creator, reconciler, statementOfAccountDate, notes, status from aw_OutsourcingStatements
    </sql>

    <select id="selectAwOutsourcingstatementsList" parameterType="AwOutsourcingstatements" resultMap="AwOutsourcingstatementsResult">
        <include refid="selectAwOutsourcingstatementsVo"/>
        <where>  
            <if test="creator != null  and creator != ''"> and creator = #{creator}</if>
            <if test="reconciler != null  and reconciler != ''"> and reconciler = #{reconciler}</if>
            <if test="statementOfAccountDate != null "> and statementOfAccountDate = #{statementOfAccountDate}</if>
            <if test="notes != null  and notes != ''"> and notes = #{notes}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectAwOutsourcingstatementsByOutsourcingStatementsID" parameterType="String" resultMap="AwOutsourcingstatementsResult">
        <include refid="selectAwOutsourcingstatementsVo"/>
        where outsourcingStatementsID = #{outsourcingStatementsID}
    </select>
        
    <insert id="insertAwOutsourcingstatements" parameterType="AwOutsourcingstatements">
        insert into aw_OutsourcingStatements
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="outsourcingStatementsID != null and outsourcingStatementsID != ''">outsourcingStatementsID,</if>
            <if test="creator != null and creator != ''">creator,</if>
            <if test="reconciler != null and reconciler != ''">reconciler,</if>
            <if test="statementOfAccountDate != null">statementOfAccountDate,</if>
            <if test="notes != null">notes,</if>
            <if test="status != null and status != ''">status,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="outsourcingStatementsID != null and outsourcingStatementsID != ''">#{outsourcingStatementsID},</if>
            <if test="creator != null and creator != ''">#{creator},</if>
            <if test="reconciler != null and reconciler != ''">#{reconciler},</if>
            <if test="statementOfAccountDate != null">#{statementOfAccountDate},</if>
            <if test="notes != null">#{notes},</if>
            <if test="status != null and status != ''">#{status},</if>
         </trim>
    </insert>

    <update id="updateAwOutsourcingstatements" parameterType="AwOutsourcingstatements">
        update aw_OutsourcingStatements
        <trim prefix="SET" suffixOverrides=",">
            <if test="creator != null and creator != ''">creator = #{creator},</if>
            <if test="reconciler != null and reconciler != ''">reconciler = #{reconciler},</if>
            <if test="statementOfAccountDate != null">statementOfAccountDate = #{statementOfAccountDate},</if>
            <if test="notes != null">notes = #{notes},</if>
            <if test="status != null and status != ''">status = #{status},</if>
        </trim>
        where outsourcingStatementsID = #{outsourcingStatementsID}
    </update>

    <delete id="deleteAwOutsourcingstatementsByOutsourcingStatementsID" parameterType="String">
        delete from aw_OutsourcingStatements where outsourcingStatementsID = #{outsourcingStatementsID}
    </delete>

    <delete id="deleteAwOutsourcingstatementsByOutsourcingStatementsIDs" parameterType="String">
        delete from aw_OutsourcingStatements where outsourcingStatementsID in 
        <foreach item="outsourcingStatementsID" collection="array" open="(" separator="," close=")">
            #{outsourcingStatementsID}
        </foreach>
    </delete>
</mapper>